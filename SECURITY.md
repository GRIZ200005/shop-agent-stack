# Security / 安全说明

## 支持范围

当前项目面向本机开发和合成业务演示，没有独立安全审计、生产SLA或已维护的多版本安全支持矩阵。默认Compose和体验验证码机制不适合直接向公网提供商业服务。

## 报告问题

不要在公开Issue或PR里粘贴凭据、真实个人信息、可直接利用的攻击细节或数据库导出。如果仓库启用了GitHub Private Vulnerability Reporting，使用其私密报告入口；否则先通过维护者提供的私密联系方式协调。当前文档不承诺该GitHub功能已启用，也不公布未经确认的联系邮箱或响应时限。

报告应包含受影响提交、最小复现、所需权限、影响与建议修复；只使用自己创建的合成账户和数据。

## 凭据与运行数据

- `.env`、`.local`、API Key、JWT、部署加密密钥、数据库和会话数据均不得提交。
- Agent加密密钥与加密配置需要对应备份。密钥丢失不应通过弱化加密绕过。
- 模型请求可能包含用户问题和业务工具结果。仅向你信任且有权使用的服务发送合成测试数据。
- 发布截图前检查账号、地址、模型配置、浏览器侧栏和本机路径；忽略文件规则不能清除Git历史中的已提交信息。
- 若发现真实密钥曾被提交，应先撤销/轮换，再协调历史清理；仅删除当前文件不能消除风险。

## 部署边界

保持宿主机端口绑定回环地址，避免暴露Redis、MongoDB、RabbitMQ、Milvus及依赖组件。生产化需另外完成TLS、网络隔离、角色审计、备份恢复、依赖扫描、监控和容量验证。自定义模型连接的地址校验及关键写入确认机制不能为了连接方便而直接关闭。

发布前流程见 [公开检查](docs/public-release.md)。
