# Contributing / 贡献指南

感谢你愿意改进Aster Commerce。请先阅读 [README](README.md) 和 [架构说明](docs/architecture.md)，区分当前功能与历史规划。

## 提交问题

提供复现步骤、预期/实际结果、提交版本、操作系统、Docker/Node版本和必要的脱敏日志。说明问题发生在哪个角色、页面或API；不附API Key、密码、JWT、真实地址、原始对话数据库或`.local`目录。漏洞与敏感数据问题参见 [SECURITY.md](SECURITY.md)。

## 开发流程

1. 为具体问题建立小范围分支，先确认已有实现和测试。
2. 修改与问题相关的代码，保留上游作者注释与修改说明。
3. 运行与风险相称的验证；UI/业务集成可使用 `apps/web` 下的 `npm run test:v1`，前提见 [测试指南](docs/testing.md)。
4. PR描述说明触发条件、修改后的行为、测试结果和仍存在的限制。失败后重试要保留原因，不能只给最终成功数字。
5. 涉及数据库迁移、来源、数据或部署步骤时同步更新文档。

不要求每个文案或样式改动都跑全量模型/故障评测。付费模型调用必须是明确选择的验证，不混入默认测试；不要用真实客户数据作为夹具。

## 必须保留的边界

- Java校验业务归属和角色，模型输入不构成授权。
- 敏感Agent写入需要后端保存的确认状态。
- 价格/库存/政策以权威来源为准，旧上下文不替代查询。
- 幂等、事务、Outbox和事件状态不可为“演示成功”而绕过。
- 不导入无权公开的代码、数据、图片、配置或历史。
- 上游代码保留适用的许可证/声明；新增依赖说明用途和来源。

## 许可证

本项目原创代码与文档采用 [Apache-2.0](LICENSE)。提交原创贡献时应有权按项目许可证提供；第三方材料必须保留原许可与来源，不能仅通过改名变为原创。尚未配置强制CI门禁或CLA流程，不应在PR中声称这些检查已自动完成。
